import { StyleSheet, Text, View } from 'react-native'
import React, { useEffect } from 'react'

const optionPane = ({ options }) => {

    useEffect({

    }, [options]);
  return (
    <View>
      
    </View>
  )
}

export default optionPane

const styles = StyleSheet.create({})